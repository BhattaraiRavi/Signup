<div class="container">
    <div class="row clearfix well">
    	<% if @participant.errors.any? %>
            <div id="error_explanation">
            <h2><%= pluralize(@participant.errors.count, "error") %> prohibited you from being registered.</h2>
            <ul>
              <% @participant.errors.full_messages.each do |message| %>
              <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <%= link_to "Try Again", event_path, :class=>"btn btn-primary", :disabled => false %>
    </div>
</div
